for (var i = 0; i < test_languages.length; i++) {            
  retval = doBenchmark(test_languages[i]['plaintext_regex'], test_languages[i]['plaintext_max_len'],
                       test_languages[i]['ciphertext_regex'], test_languages[i]['ciphertext_max_len']);
  console.log(retval);
}
